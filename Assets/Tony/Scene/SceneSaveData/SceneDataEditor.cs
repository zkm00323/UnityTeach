using UnityEditor;
using UnityEngine;

[CustomEditor(typeof(SceneData))]
class SceneDataEditor : Editor {
	/*public override void OnInspectorGUI() {
		base.OnInspectorGUI();
		if(GUILayout.Button("存檔")){
			//SceneCtrl.Instance.Save((SceneData)target);
		}
	} */
}